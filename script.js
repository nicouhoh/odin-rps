//get computer choice
function computerChoice() {
  let cpuNumber = Math.random();
  return cpuNumber < 1 / 3 ? "rock" : cpuNumber < 2 / 3 ? "paper" : "scissors";
}
// get human choice
//
// declare t
